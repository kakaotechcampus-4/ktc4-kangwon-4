import { AppShell } from '../components/AppShell'

/** ⑤ 재계획 결과 — 구현 예정 */
export function ReplanPage() {
  return (
    <AppShell title="다시 계산했습니다">
      <p className="text-base text-gray-500">준비 중입니다.</p>
    </AppShell>
  )
}

import { AppShell } from '../components/AppShell'

/** ④ 충돌 확인 — 구현 예정 */
export function ConfirmChangePage() {
  return (
    <AppShell title="확인이 필요합니다">
      <p className="text-base text-gray-500">준비 중입니다.</p>
    </AppShell>
  )
}

# CLAUDE.md — RE:BORN 팀 공통

FE / BE / AI가 함께 지킬 **최소한**만 담습니다. 이 문서의 목적은 규칙을 늘리는 게 아니라,
에이전트에게 **이 프로젝트의 맥락을 연결**하는 것입니다.

**개인 개발환경에는 관여하지 않습니다.** 각자의 `~/.claude/` 설정·스킬·훅·서브에이전트,
개인 CLAUDE.md는 자유입니다. 개인 환경에서 잘 통한 방법이 팀에도 필요해 보이면 그때 안건으로
올립니다. 공통으로 강제하는 워크플로우는 만들지 않습니다.

화면·API·스키마·지표처럼 구현하며 바뀌는 것은 PRD와 `docs/`에 둡니다. 충돌하면 **방향은 이 문서,
세부는 그 문서**를 따르고, 방향이 틀렸다고 생각되면 코드로 우회하지 말고 PM에게 말합니다.

규칙은 리뷰에서 **같은 지적이 두 번 나올 때** 추가합니다. 미리 채우지 않습니다. 이 문서의 DRI는
PM이고, API 계약·보안·아키텍처에 영향을 주는 변경은 관련 파트 리뷰 후 반영합니다.
파트 문서는 각 파트가 갖습니다.

## 무엇을 만드는가

폐업을 결심한 임차형 소규모 카페 사업자(1~5인·비프랜차이즈)를 위한 서비스입니다.
임대차·원상복구·철거·지원·세무가 서로를 잠그고 있어서, **한 조건이 풀릴 때마다 나머지 계획
전체를 혼자 다시 맞춰야 하는 문제**를 없앱니다.

사용자는 이미 폐업을 결정한 상태로 들어옵니다. 계획서를 만들어주는 게 아니라, 점포 반환·정리·
지원·행정이 실제로 끝날 때까지 하나의 **Closure Case**로 따라갑니다.

```
Case 생성 → Blocker 1개 판단 → Next Action 1개 제시
  → 사용자가 현실에서 실행 → 결과 입력 → Case 상태 변경 → 재계획 → (반복)
```

**모든 기능은 이 루프의 한 단계입니다.** 무엇을 만들지 애매하면 "이게 루프의 어느 단계냐"를
먼저 답합니다. 답이 안 나오면 지금 만들 것이 아닙니다.

## 무엇을 성공으로 보는가

계획을 잘 만들었는지가 아니라 **Case가 실제로 진척됐는지**로 판단합니다.
같은 Case에서 **현실 행동의 결과 입력으로 재계획이 실제로 일어나는 것**이 기준입니다.
첫 계획 생성만으로는 아무것도 검증되지 않습니다.

폐업은 행동 사이 간격이 며칠입니다. 한 자리에서 몇 번 돌았는지가 아니라 Case 단위로 봅니다.
목표 수치와 측정 방식은 PRD에 둡니다.

## 하지 않는 것

아직 못 만든 기능이 아니라 **의도적으로 만들지 않기로 한 범위**입니다.

- 폐업 여부를 판단하지 않는다 — 사용자는 이미 결정한 상태로 들어온다
- 최적 폐업일을 추천하지 않는다
- 공식 정보나 코드 계산 근거 없이 폐업일·신청일·신고기한 등 도메인 날짜를 만들거나 추정하지 않는다
- 법률·세무·채무·지원자격의 최종 판단을 하지 않는다
- 신청·계약·서명·외부 연락·금전 이동을 실행하지 않는다
- 인수자 등장 시점·권리금 회수액·분쟁 결과를 예측하지 않고, 인수자·권리금·전문가·철거업체를 매칭하지 않는다
- 재취업·재창업 기능, 커뮤니티·후기를 만들지 않는다
- 전 업종·전체 지원사업으로 넓히지 않고, 계약서를 자동 분석하지 않는다
- 정책 변경을 자동 감지하지 않는다 — 정책 데이터는 팀이 검수한다

**"나중에"가 아니라 "지금은 아니다"입니다.** 필요해 보이면 추가하지 말고 안건으로 올립니다.
범위가 계속 커지는 것이 이 프로젝트의 가장 큰 리스크입니다.

## 누가 판단하는가

- **Case가 Source of Truth다.** 판단은 검증된 Case 상태에서 출발한다
- **Rule / 코드** — 상태 전이 유효성, 절차 적용 조건, 선행조건을 검증해 **후보를 만든다.**
  선후관계를 위반하는 행동은 후보에서 제외된다
- **Agent / Planner** — 검증된 후보 중에서 현재 Blocker와 Next Action을 **선택한다.**
  Rule이 제외한 후보를 되살릴 수 없다
- **LLM** — 자연어 해석, 비정형 문서 구조화, 근거 탐색, 사용자에게 설명하기
- **FE** — 입력 전달, 결과 표시, 충돌 확인 UI. **자격·우선순위·기한을 계산하지 않는다**
- **사람(팀)** — 정책 데이터 검수, 지원 자격의 최종 해석

**사용자에게 나가는 Blocker와 Next Action의 최종 결정 지점은 하나입니다.** 여러 곳에서 각자 다음
행동을 내놓으면 "혼자 계획을 다시 맞춰야 하는 문제"를 우리가 다시 만드는 것입니다.
그리고 **판단마다 무엇을 근거로 그랬는지 남깁니다.**

Agent를 몇 개 어떤 경계로 둘지는 구현 결정이므로 `docs/architecture.md`에서 관리합니다.

어느 층에도 두지 않는 것:

- LLM이 Case 상태를 직접 변경하는 코드
- LLM이 지원 자격·법률·세무를 단독으로 확정하는 코드
- LLM이 금액·기간·기한을 만들거나 추정하는 코드 — 공식 데이터나 코드 계산 결과만 쓴다
- 조회·검증된 `support_item`에 없는 지원사업명이나 조건을 생성하는 코드

## 뚫리면 안 되는 선

잘하면 좋은 게 아니라, 뚫리면 내보낼 수 없는 것들입니다.

1. **절차상 틀린 Next Action** — 그래서 선후관계는 Rule이 후보 단계에서 걸러낸다
2. **근거 없는 단정** — 법률·세무·지원 자격은 "확인 필요"로 두고 전문가·공식기관 확인을 다음 행동으로 낸다
3. **모르는 것을 아는 척하기** — 확인 안 된 값은 `UNKNOWN`으로 유지한다. 추정값을 채우거나 빈 값으로
   숨기지 않고, 충돌하는 정보를 자동으로 덮어쓰지 않는다
4. **되물어야 이해되는 말** — 중장년 사용자 기준으로 전문용어를 쓰지 않는다
5. **왜 그 판단이 나왔는지 못 밝히는 상태**

한 화면에서 강조하는 것은 **Blocker 1개, Next Action 1개**입니다.

아래는 문서가 아니라 **코드·validation·테스트로도** 막습니다 — 인증/인가와 접근 권한,
개인정보 보호, Case 상태 전이 제한, Conflict 자동 덮어쓰기 방지, 외부 전달 승인.

## 개인정보

- 주민번호·계좌번호 입력 필드를 만들지 않는다. 사용자는 본인 Case만 조회할 수 있다
- 계약서 저장소를 인증 없이 공개하지 않고, 계약서 원문과 사업장 주소를 로그에 남기지 않는다
  — 필요하면 참조 ID를 기록한다. 외부 전달이 발생하면 무엇을 언제 전달했는지 남긴다
- 실제 계약서·사업자 정보를 커밋·이슈·PR에 올리지 않는다. 테스트는 가짜 데이터로 한다
- 키와 토큰은 `.env`로 관리하고 `.env.example`만 커밋한다. 로그·응답에 토큰을 싣지 않는다

## 같은 말을 쓴다

| 용어 | 뜻 |
|---|---|
| Closure Case | 한 사업자의 폐업 과정 전체를 담는 단위. Source of Truth |
| Blocker | 지금 다음 행동을 막고 있는 조건 1개. 저장된 속성이 아니라 매번 판단한 결과 |
| Next Action | 사용자가 현실에서 할 일 1개. 무엇을·왜·무엇을 확인해야 하는지가 다 들어간다 |
| Re-planning | 결과가 입력되면 기존 계획을 다시 검토해 Blocker와 Next Action을 다시 내는 것 |
| Conflict | 새 입력이 기존 값과 충돌한 상태. 사용자가 고르기 전까지 반영하지 않는다 |
| UNKNOWN | 아직 확인되지 않음. "없음"이 아니다 |

JSON API 필드는 `camelCase`, DB 컬럼은 `snake_case`. `Task` 대신 `Next Action`을 씁니다.
"추천"을 불필요하게 쓰지 않고, "지원 가능"·"지원금 수령 확정" 대신 "검토 대상"·"추가 확인 필요"를
씁니다. 새 개념이 필요하면 각자 이름 붙이지 말고 이 표에 추가합니다.

## 정해지지 않은 것을 만났을 때

- 정해진 것처럼 코드를 쓰지 말고, 가정을 `// TODO`로 적고 진행하는 편이 좋습니다
- 확정되지 않은 API 필드를 optional로 조용히 추가하지 않습니다 — 응답이 비어도 그냥 넘어가서
  버그가 숨습니다. 필요하면 계약 확정을 요청합니다
- 결정이 나면 문서를 고칩니다. 세부는 PRD와 `docs/`, 방향이 바뀐 것이면 이 문서

## 어디를 보면 되는가

작업에 필요한 것만 봅니다.

- API 추가·변경 전 `docs/interface-spec.md` / Case 상태 구조 변경 전 `docs/data-model.md`
- 시스템 경계·Agent 구성 변경 전 `docs/architecture.md` / 사용자 흐름 변경 전 `docs/hero-scenario.md`
- PRD(Notion) — 요구사항·지표·리스크 원본. **초안이며 계속 바뀝니다**

`main`·`develop`에 직접 커밋하지 않습니다 (상세는 `docs/git-convention.md`).
파트 사이의 계약(API·데이터 모델·용어)은 한쪽 폴더가 아니라 루트 `docs/`에 둡니다.

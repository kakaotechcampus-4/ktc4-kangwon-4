# CLAUDE.md — RE:BORN 팀 공통 원칙

FE / BE / AI 전체에 공통으로 적용되는 방향과 원칙입니다.
파트에서만 필요한 구현 규칙은 `frontend/CLAUDE.md`, `backend/CLAUDE.md`에 둡니다.

이 문서는 **잘 바뀌지 않는 것**만 담습니다. 화면·API·스키마·일정처럼 구현하면서 바뀌는 것은
PRD와 `docs/`에 둡니다. 이 문서와 다른 문서가 충돌하면 **방향은 이 문서, 세부는 그 문서**를
따르고, 방향 자체가 틀렸다고 생각되면 코드로 우회하지 말고 PM에게 말합니다.

## 무엇을 만드는가

폐업을 결심한 임차형 소규모 카페 사업자(1~5인·비프랜차이즈)를 위한 서비스입니다.
임대차·원상복구·철거·지원·세무가 서로를 잠그고 있어서, **한 조건이 풀릴 때마다 나머지 계획
전체를 혼자 다시 맞춰야 하는 문제**를 없앱니다.

사용자는 이미 폐업을 결정한 상태로 들어옵니다. 우리는 계획서를 만들어주는 게 아니라,
점포 반환·정리·지원·행정이 실제로 끝날 때까지 하나의 **Closure Case**로 따라갑니다.

## Hero Loop

```
Case 생성 → Blocker 1개 판단 → Next Action 1개 제시
  → 사용자가 현실에서 실행 → 결과 입력 → Case 상태 변경 → 재계획 → (반복)
```

**모든 기능은 이 루프의 한 단계입니다.** 무엇을 만들지 애매하면 "이게 루프의 어느 단계냐"를
먼저 답합니다. 답이 안 나오면 지금 만들 것이 아닙니다.

## 성공은 하나로만 판단한다

> 실제 사용자 세션에서 이 루프가 **2회전 이상** 도는 것.

1회전은 첫 계획을 만든 것뿐이고, 재계획은 2회전부터 검증됩니다. 만족도·완료율·재방문율·체류시간은
MVP에서 측정하지 않습니다. 이 지표를 올리지 않는 작업은 우선순위가 아닙니다.

## 하지 않는 것

아직 구현하지 못한 기능이 아니라 **의도적으로 만들지 않기로 한 범위**입니다.

- 폐업 여부를 판단하지 않는다 — 사용자는 이미 결정한 상태로 들어온다
- 최적 폐업일을 추천하지 않는다
- 공식 정보나 코드 계산 근거 없이 폐업일·신청일·신고기한 등 도메인 날짜를 만들거나 추정하지 않는다
- 법률·세무·채무·지원자격의 최종 판단을 하지 않는다
- 신청·계약·서명·외부 연락·금전 이동을 실행하지 않는다
- 인수자 등장 시점·권리금 회수액·분쟁 결과를 예측하지 않는다
- 인수자·권리금을 매칭하지 않는다
- 전문가·철거업체를 매칭하지 않는다
- 재취업·재창업 기능을 만들지 않는다
- 전 업종·전체 지원사업으로 넓히지 않는다
- 계약서를 자동 분석하지 않는다
- 정책 변경을 자동 감지하지 않는다 — 정책 데이터는 팀이 검수한다
- 에이전트를 여러 개로 나누지 않는다 — 나눌 조건은 `## 누가 판단하는가` 참고
- 커뮤니티·후기·리뷰를 만들지 않는다

**"나중에"가 아니라 "지금은 아니다"입니다.** 필요해 보이면 추가하지 말고 안건으로 올립니다.
범위가 계속 커지는 것이 이 프로젝트의 가장 큰 리스크입니다.

## 누가 판단하는가

판단 주체는 **Supervisor Agent 1개 + Tool / Rule** 하나입니다. 에이전트를 여러 개로 나누지 않습니다.

판단 영역이 임대차·철거·지원·세무로 나뉘어 있어도, 각 영역의 판단은 **"한 번 물어보면 끝나는"**
형태로 Tool과 Rule로 표현됩니다. 자기 안에서 여러 번 돌아야 하는 독립된 판단 루프가 아니면
에이전트로 뗄 이유가 없고, 늘리는 만큼 호출·응답시간·비용이 늘고 **어느 판단이 이 결과를
만들었는지 추적하기 어려워집니다.**

**나눌 조건이 생기면 그때 재검토합니다.** 예를 들어 지원사업 탐색이 "검색 → 분석 → 재검색 →
검증"처럼 스스로 여러 번 돌아야 하는 형태가 되면, 그 영역만 별도 에이전트로 떼는 것을 다시 봅니다.
그때도 기준은 역할 이름이 아니라 **독립된 판단 루프인지**입니다.

에이전트가 하나든 여럿이든 유지되는 것:

- **사용자에게 나가는 Blocker와 Next Action은 한 곳에서만 확정한다.** Tool과 Rule은 근거와
  후보를 만들고, 최종 1개는 Supervisor가 고릅니다. 여러 곳에서 각자 다음 행동을 내놓으면
  "혼자 계획을 다시 맞춰야 하는 문제"를 우리가 다시 만드는 것입니다.
- **판단마다 무엇을 근거로 그랬는지 남긴다.**

층별 책임:

- **Case가 Source of Truth다.** 판단은 검증된 Case 상태에서 출발한다
- **Rule / 코드**: 상태 전이 유효성, 절차 적용조건, 선행조건 — 결정적인 것
- **Agent / LLM**: 자연어 해석, 비정형 문서 구조화, 근거 탐색, 사용자에게 설명하기 — 애매한 것
- **FE**: 입력 전달, 결과 표시, 충돌 확인 UI. **자격·우선순위·기한을 계산하지 않는다**
- **사람(팀)**: 정책 데이터 검수, 지원 자격의 최종 해석

어느 층에도 두지 않는 것:

- LLM이 Case 상태를 직접 변경하는 코드
- LLM이 지원 자격·법률·세무를 단독으로 확정하는 코드
- LLM이 금액·기간·기한을 만들거나 추정하는 코드 — 공식 데이터나 코드 계산 결과만 씁니다
- 조회·검증된 `support_item`에 없는 지원사업명이나 조건을 생성하는 코드

## 뚫리면 안 되는 선

잘하면 좋은 게 아니라, 하나라도 뚫리면 내보낼 수 없는 것들입니다.

1. **절차상 명백히 틀린 Next Action** — 0건. 그래서 선후관계는 LLM이 아니라 Rule이 판단합니다.
2. **근거 없는 단정** — 0건. 법률·세무·지원 자격은 "확인 필요"로 두고 전문가·공식기관 확인을
   다음 행동으로 냅니다.
3. **모르는 것을 아는 척하지 않기** — 확인되지 않은 값은 `UNKNOWN`으로 유지합니다. 추정값을
   채우거나 빈 값으로 숨기지 않습니다. 충돌하는 정보를 자동으로 덮어쓰지 않습니다.
4. **되물어야 이해되는 말** — 세션당 2회 이하. 중장년 사용자 기준으로 전문용어를 쓰지 않습니다.
5. **왜 그런 판단이 나왔는지 못 밝히는 상태** — 판단 근거를 추적할 수 있게 남깁니다.

한 화면에서 강조하는 것은 **Blocker 1개, Next Action 1개**입니다. 여러 개를 동시에 보여주는
순간 "혼자 다시 맞춰야 하는 문제"를 우리가 다시 만드는 것입니다.

## 문서만으로 강제하지 않는다

이 문서는 원칙일 뿐이고 시스템 장치가 아닙니다. 아래는 **코드·validation·테스트로도** 막습니다.

- 인증 / 인가, 접근 권한
- 개인정보 보호
- Case 상태 전이 제한
- Conflict 자동 덮어쓰기 방지
- 외부 전달 승인

## 개인정보

- 주민번호·계좌번호 입력 필드를 만들지 않는다
- 사용자는 본인의 Case만 조회할 수 있어야 한다
- 계약서 저장소를 인증 없이 공개하지 않는다
- 계약서 원문과 사업장 주소를 로그에 남기지 않는다 — 필요하면 원문 대신 참조 ID를 기록한다
- 외부 전달이 발생하면 무엇을 언제 전달했는지 기록한다
- 실제 계약서·사업자 정보를 커밋·이슈·PR에 올리지 않는다. 테스트는 가짜 데이터로 한다
- API 키와 토큰은 `.env`로 관리하고 `.env.example`만 커밋한다. 로그·응답에 토큰을 싣지 않는다

## 같은 말을 쓴다

| 용어 | 뜻 |
|---|---|
| Closure Case | 한 사업자의 폐업 과정 전체를 담는 단위. Source of Truth |
| Blocker | 지금 다음 행동을 막고 있는 조건 1개. 저장된 속성이 아니라 매번 판단한 결과 |
| Next Action | 사용자가 현실에서 할 일 1개. 무엇을·왜·무엇을 확인해야 하는지가 다 들어간다 |
| Re-planning | 결과가 입력되면 기존 계획을 다시 검토해 Blocker와 Next Action을 다시 내는 것 |
| Conflict | 새 입력이 기존 값과 충돌한 상태. 사용자가 고르기 전까지 반영하지 않는다 |
| UNKNOWN | 아직 확인되지 않음. "없음"이 아니다 |

표기:

- JSON API 필드는 `camelCase`, DB 컬럼은 `snake_case`
- `Task` 대신 **`Next Action`**을 씁니다
- 제품 표현에서 "추천"이라는 단어를 불필요하게 쓰지 않습니다
- "지원 가능", "지원금 수령 확정"처럼 확정으로 오해될 표현을 쓰지 않고,
  "검토 대상", "추가 확인 필요"를 씁니다

새 개념이 필요하면 각자 이름 붙이지 말고 이 표에 추가합니다.

## 정해지지 않은 것을 만났을 때

이 프로젝트는 결정보다 미정이 많은 단계입니다. 미정을 만나면:

1. **정해진 것처럼 코드를 쓰지 않는다.** 가정을 `// TODO`로 적고 그대로 진행합니다.
2. **막히면 멈추지 말고 물어본다.** 확정을 기다리는 동안 다른 부분을 먼저 끝냅니다.
3. **확정되지 않은 필드를 optional로 조용히 추가하지 않는다.** 응답이 비어도 그냥 넘어가서
   버그가 숨습니다. 필요하면 계약 확정을 요청합니다.
4. **결정이 나면 문서를 고친다.** 세부는 PRD와 `docs/`, 방향이 바뀐 것이면 이 문서.
   슬랙에만 남기지 않습니다.

## 이 문서를 다루는 법

- 규칙은 **리뷰에서 같은 지적이 두 번 나올 때** 한 줄 추가합니다. 미리 채우지 않습니다.
- 어겨도 되는 것은 넣지 않습니다. 권장 사항이 섞이면 전체가 같이 무시됩니다.
- 코딩 스타일·네이밍·포맷은 여기 두지 않습니다. 린터와 리뷰의 몫입니다.
- `.claude/rules/`, `.claude/skills/`는 초기에 만들지 않습니다. 필요성이 확인될 때 추가합니다.
- **개인 개발환경은 각자 자유입니다.** 이 문서는 팀이 함께 지킬 최소한이고, 각자의 `~/.claude/`
  설정·스킬·훅·서브에이전트에는 관여하지 않습니다. 개인 환경에서 잘 통한 방법이 팀에도
  필요하다고 판단되면 그때 안건으로 올립니다.

## 레포 운영

- `main` — 보호 브랜치. 직접 커밋하지 않습니다. 멘토 리뷰용 `develop → main` PR로만 반영합니다
- `develop` — 기본 통합 브랜치. 여기도 직접 커밋하지 않습니다
- `feature/<이슈명>` — 기능 단위 작업 / `refactor/<내용>` — 멘토 리뷰 피드백 반영
- 커밋 메시지는 `<prefix>: <내용>` 형식. PR 본문에는 변경 요약 / 리뷰 포인트 / 고민한 설계·예외를 씁니다
- 상세는 `docs/git-convention.md`를 봅니다 (모순되면 그 문서 기준)

## 어디를 보면 되는가

모든 문서를 항상 읽지 않습니다. 작업에 필요한 것만 봅니다.

- API를 추가·변경하기 전에 `docs/interface-spec.md`
- Case 상태 구조를 바꾸기 전에 `docs/data-model.md`
- 시스템 경계를 바꾸기 전에 `docs/architecture.md`
- Hero Loop나 사용자 흐름을 바꾸기 전에 `docs/hero-scenario.md`
- 파트별 개발 규칙은 `frontend/CLAUDE.md`, `backend/CLAUDE.md`
- 설치·실행·현재 구성은 각 파트의 `README.md`
- PRD(Notion) — 요구사항·리스크 원본. **초안이며 계속 바뀝니다**

**CLAUDE.md = 지켜야 할 것, README.md = 사실.** 섞지 않습니다.
파트 사이의 계약(API·데이터 모델·용어)은 한쪽 폴더가 아니라 루트 `docs/`에 둡니다.

## 문서 소유권

| 문서 | DRI | 비고 |
|---|---|---|
| `/CLAUDE.md` | PM | API 계약·보안·아키텍처에 영향을 주는 변경은 관련 파트 리뷰 후 반영 |
| `frontend/CLAUDE.md` | FE | |
| `backend/CLAUDE.md` | BE | |
| `docs/interface-spec.md` | FE/BE 합의 | 최종 정리는 BE — 변경 권한과 정리 담당을 분리 |
| `docs/data-model.md` | AI 리드 | 변경 시 BE 리뷰 |
| `docs/architecture.md` | AI 리드 | 변경 시 BE 리뷰 |
| `docs/hero-scenario.md` | PM / AI 리드 협의 | |
| `docs/tech-stack.md` | AI 리드 | 확정 사실 갱신. 원문(CLAUDE.md들)과 충돌하면 원문 우선 |
| `docs/git-convention.md` | AI 리드 | 팀 논의로 변경 가능 |

# CLAUDE.md — RE:BORN 공통 원칙

이 문서는 프론트엔드 / 백엔드 / Agent 전체에 공통으로 적용되는 원칙입니다.
각 파트에서만 필요한 구현 규칙은 `frontend/CLAUDE.md`, `backend/CLAUDE.md`를 참고하세요.

## 서비스

RE:BORN은 폐업을 결심한 소상공인의 폐업 과정을 하나의 **Closure Case**로 관리하는 Agent입니다.
조건이 바뀔 때마다 현재 진행을 막는 요소와 다음 행동을 다시 계산합니다.

MVP 타깃: 1~5인 · 비프랜차이즈 · 임차형 · 카페 사업자

## Hero Loop

```
Case 생성
→ Blocker 1개 판단
→ Next Action 1개 제시
→ 사용자가 현실에서 실행
→ 결과 입력
→ Case 상태 변경
→ 재계획
→ 반복
```

새 기능을 추가하기 전에 해당 기능이 이 루프를 강화하는지 확인합니다.

## 하지 않는 것

다음 항목은 아직 구현하지 못한 기능이 아니라 **의도적으로 만들지 않기로 한 범위**입니다.

- 폐업 여부를 판단하지 않는다
- 최적 폐업일을 추천하지 않는다
- 공식 정보 또는 코드 계산 근거 없이 폐업일·신청일·신고기한 등 도메인 날짜를 생성하거나 추정하지 않는다
- 법률·세무·채무·지원자격의 최종 판단을 하지 않는다
- 신청·계약·서명·외부 연락·금전 이동을 실행하지 않는다
- 인수자 등장 시점·권리금 회수액·분쟁 결과를 예측하지 않는다
- 인수자·권리금을 매칭하지 않는다
- 재취업·재창업 기능을 만들지 않는다

## 역할 경계

- Case를 Source of Truth로 사용한다
- 상태 전이 유효성, 절차 적용조건, 선행조건은 Rule / 코드가 검증한다
- Agent / Planner는 검증된 Case 상태와 Rule 결과를 바탕으로 현재 Blocker와 Next Action을 판단한다
- LLM이 Case 상태를 임의로 직접 변경하지 않는다
- 금액·기간·기한을 LLM이 임의 생성하거나 추정하지 않는다
- 금액·기간·기한은 공식 데이터 또는 코드 계산 결과만 사용한다
- 조회·검증된 `support_item`에 존재하지 않는 지원사업명이나 조건을 생성하지 않는다
- 정보가 없으면 추측하지 않는다
- 확인되지 않은 값은 `UNKNOWN`으로 유지한다
- 충돌하는 정보는 자동으로 덮어쓰지 않는다
- 판단 근거를 추적할 수 있도록 남긴다

## 개인정보

- 주민번호·계좌번호 입력 필드를 만들지 않는다
- 계약서 저장소를 인증 없이 공개하지 않는다
- 사용자는 본인의 Case만 조회할 수 있어야 한다
- 계약서 원문과 사업장 주소를 로그에 남기지 않는다
- 필요한 경우 원문 대신 참조 ID를 기록한다
- 외부 전달이 발생하면 무엇을 언제 전달했는지 기록한다

## 용어

- JSON API 필드: `camelCase`
- DB 컬럼: `snake_case`
- `Task` 대신 `Next Action` 사용
- 제품 표현에서 불필요하게 "추천"이라는 단어를 사용하지 않는다
- "지원 가능", "지원금 수령 확정"처럼 확정적으로 오해될 수 있는 표현을 피한다 — 대신 "검토 대상", "추가 확인 필요" 등을 사용한다

## 참조 문서

Claude가 모든 문서를 항상 읽도록 강제하지 않습니다. 작업에 필요한 문서만 확인합니다.

- API를 추가하거나 변경하기 전에 `docs/interface-spec.md`를 확인한다
- Case 상태 구조를 변경하기 전에 `docs/data-model.md`를 확인한다
- 시스템 경계를 변경하기 전에 `docs/architecture.md`를 확인한다
- Hero Loop 또는 사용자 흐름을 변경하기 전에 `docs/hero-scenario.md`를 확인한다

## 시스템 invariant는 문서만으로 강제하지 않는다

CLAUDE.md는 Claude Code가 따라야 할 원칙일 뿐, 보안이나 상태 전이를 강제하는 시스템 장치가 아닙니다.
다음 항목은 코드 / validation / test로도 실제로 강제합니다.

- 인증 / 인가
- 개인정보 보호
- Case 상태 전이 제한
- Conflict overwrite 방지
- 접근 권한
- 외부 전달 승인

## 레포 운영

- `main`: 보호 브랜치 — 직접 커밋하지 않습니다
- `develop`: 기본 통합 브랜치 — 여기도 직접 커밋하지 않고 feature/refactor 브랜치에서 작업 후 PR로 합칩니다
- `feature/<이슈명>`
- `refactor/<내용>`
- 커밋 메시지 prefix 규칙은 `docs/git-convention.md` 참고

PR 본문에는 변경 요약 / 리뷰 포인트 / 설계·예외 사항을 포함합니다.

## 문서 소유권

| 문서 | DRI | 비고 |
|---|---|---|
| `/CLAUDE.md` | PM | API 계약·보안·아키텍처에 영향을 주는 변경은 관련 파트 리뷰 후 반영 |
| `frontend/CLAUDE.md` | FE | |
| `backend/CLAUDE.md` | BE | |
| `docs/interface-spec.md` | FE/BE 합의 | 최종 정리는 BE 제안 — 내용 변경 권한과 정리 담당을 분리 |
| `docs/data-model.md` | AI 리드 | 변경 시 BE 리뷰 |
| `docs/architecture.md` | AI 리드 | 변경 시 BE 리뷰 |
| `docs/hero-scenario.md` | PM/AI 리드 협의 | |
| `docs/tech-stack.md` | AI 리드 | 스택 버전 등 확정 사실 갱신, 팀 공유용 요약이므로 원문(CLAUDE.md들)과 충돌 시 원문 우선 |
| `docs/git-convention.md` | AI 리드 | 팀 논의로 변경 가능 |

`.claude/rules/`, `.claude/skills/`는 초기에는 만들지 않습니다. 실제 개발 과정에서 필요성이 확인될 때 추가합니다.

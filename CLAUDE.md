# CLAUDE.md — RE:BORN 팀 공통

## 이 프로젝트를 왜 하는가

**포트폴리오입니다.** 서비스를 완성하는 것보다, 우리가 만든 걸 우리가 설명할 수 있는 게 먼저입니다.
면접에서 "이거 왜 이렇게 만드셨어요?"에 대답 못 하면, 그 코드는 없는 것과 같습니다.

그래서 이 문서의 목적은 규칙을 늘리는 게 아니라, **AI가 우리를 앞질러 가지 않게 속도를 맞추는 것**입니다.
지금까지의 문제는 코드가 부족한 게 아니라, 아무도 다 이해하지 못하는 코드와 문서가 너무 많다는 것이었습니다.

판단이 애매할 때 기준은 하나입니다. **담당자가 남에게 설명할 수 있는가.** 아니면 하지 않습니다.

## Claude에게 (이 항목이 가장 중요합니다)

1. **시키지 않은 파일을 만들지 않는다.** 특히 설계 문서·스펙 문서·요약 md. 필요해 보이면 만들지 말고 먼저 묻는다
2. **한 번에 많은 코드를 쏟아내지 않는다.** 한 번에 검토할 수 있는 크기(수십 줄)로 끊어서 낸다
3. **스키마·필드·기능을 혼자 판단해서 추가하지 않는다.** 없으면 넘어가지 말고 담당자에게 묻는다
4. **새 용어를 만들지 않는다.** 영어 약어·새 이름이 필요하면 붙이기 전에 묻고, 쓸 때는 한 줄로 풀어 쓴다
5. **답변은 담당자가 그대로 팀에 설명할 수 있는 말로 쓴다.** 전공자만 아는 단어로 쓰지 않는다
6. **모르면 추측해서 진행하지 말고 멈추고 질문한다.** 가정이 필요하면 `// TODO`로 남긴다

개인 `~/.claude/` 설정·스킬·서브에이전트는 각자 자유입니다. 공통으로 강제하는 워크플로우는 만들지 않습니다.

## 어떻게 만드는가 — 작게, 대신 끝까지 이어서

MVP를 한 번에 만들지 않습니다. **한 번에 기능 하나씩, 대신 FE→BE→AI→FE가 다 이어지게** 만듭니다.

첫 단계 예시:

```
입력 폼 하나 있는 화면(FE)
  → 입력을 BE가 받음
  → BE가 AI에 넘김
  → AI가 응답을 만들어 BE에 돌려줌
  → BE가 FE에 주고 화면에 출력
```

FE·BE·AI 각각 최소한만 만든 이 상태를 먼저 만들고, 여기에 하나씩 붙입니다.
이렇게 하면 매번 이해해야 할 대상이 작아지고 명확해집니다.

- 기능을 다 완성해서 올리지 않습니다. **이어지면 올립니다**
- 하나의 작업을 오래 붙잡지 않습니다. 막히면 그날 안에 공유합니다
- **오버엔지니어링 금지.** 지금 필요 없는 구조는 만들지 않습니다

## 계약은 문서가 아니라 코드로 쓴다

"계약"은 **파트끼리 주고받을 데이터의 모양을 미리 정해두는 약속**입니다. 기능마다 하나씩 만듭니다.

- **FE ↔ BE**: FastAPI 코드로 씁니다. 그 코드에서 API 명세(OpenAPI)가 자동으로 나옵니다
- **BE ↔ AI**: 주고받을 데이터를 파이썬 클래스로 씁니다. 이게 설계이자 곧 구현입니다

**설계 문서를 따로 만들지 않습니다.** 문서와 코드가 따로 있으면 둘이 어긋나고, 어긋난 걸 맞추느라 또 회의를 합니다.
코드로 쓰면 설계와 구현이 처음부터 같아서 어긋날 일이 없습니다.

계약은 **두 담당자가 직접 합의해서** 만듭니다. Claude가 만든 초안을 그대로 올리지 않습니다.
기능별 계약은 전체 계약의 축소판이라, 담당자는 자기 계약을 완전히 알고 있어야 합니다.

## PR과 리뷰

- **500줄을 넘지 않습니다.** 넘으면 쪼갭니다
- **리뷰어는 2명 이상.** 리뷰하는 습관을 같이 만듭니다
- PR 본문에 적습니다: **왜 이 코드를 넣었는지 / 왜 이 구조를 골랐는지 / 다른 방법은 뭐였고 왜 안 골랐는지 / 장점과 단점**
- **설명할 수 없으면 올리지 않습니다.** 올리기 전에 먼저 이해합니다
- `main`·`develop`에 직접 커밋하지 않습니다 (상세는 `docs/git-convention.md`)

## 소통

- **Claude가 만든 문서와 답변을 그대로 공유하지 않습니다.** 읽고, 이해하고, 내 말로 바꿔서 전달합니다
- 문서에 **모르는 단어가 나오면 그건 읽는 사람 잘못이 아니라 문서의 문제**입니다. 비전공자도 알아듣는 말로 고칩니다
- 언제든 질문하고, 언제든 상황을 브리핑합니다. 혼자 오래 들고 있는 게 가장 손해입니다

## 지금 무엇이 기준인가

- **기준은 코드입니다.** `docs/` 아래 기존 문서는 참고 자료이고, 상당 부분이 AI가 스스로 판단해 만든 것이라 현재 방향과 다를 수 있습니다. 개발 기준으로 삼지 않습니다
- **제품 범위는 다시 줄이는 중입니다.** 확정되기 전까지는 "지금 만드는 기능 하나"만 봅니다
- 새로 정해진 게 있으면 **이 문서를 고칩니다.** 이 문서를 고치고 최종 판단하는 사람은 PM입니다

## 지키는 선

문서로만이 아니라 코드·테스트로도 막습니다.

- 주민번호·계좌번호 입력 필드를 만들지 않습니다. 사용자는 본인 데이터만 조회할 수 있습니다
- 실제 계약서·사업자 정보를 커밋·이슈·PR에 올리지 않습니다. 테스트는 가짜 데이터로 합니다
- 키와 토큰은 `.env`로 관리하고 `.env.example`만 커밋합니다. 로그·응답에 토큰을 싣지 않습니다
- 확인되지 않은 값을 그럴듯하게 채우지 않습니다. 모르면 모른다고 표시합니다

---

이 방식도 해보고 이해에 도움이 안 되거나 너무 느리면 바꿉니다.
**목적은 이 문서를 지키는 게 아니라, 우리가 만든 걸 우리가 아는 상태로 끝내는 것입니다.**
